name: implement-feature
description: "Implement a feature in Go based on requirements — write idiomatic Go code following project conventions, handle errors properly, respect context cancellation, add structured logging, and ensure the implementation is testable with clear interfaces"
agent: go-developer
inputs:
  - name: requirements
    type: string
    required: true
outputs:
  - name: files_changed
    type: string[]
dependencies: []
timeout: 30m
retry:
  max_attempts: 3
  backoff: exponential
