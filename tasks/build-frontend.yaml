name: build-frontend
description: "Build frontend components and pages — implement React components with TypeScript, wire up API calls with TanStack Query, add form validation, handle loading/error states, ensure accessibility (ARIA), and style with Tailwind CSS following the project design system"
agent: vite-developer
inputs:
  - name: requirements
    type: string
    required: true
  - name: api_spec
    type: string
    required: false
outputs:
  - name: files_changed
    type: string[]
dependencies: []
timeout: 30m
retry:
  max_attempts: 2
  backoff: exponential
